<div class="container">
  <div class="adminOffer">
    <h1>Schreibe einen neuen Post</h1>
    <%= simple_form_for @article do |f| %>
      <%= f.input :title, class: "article-new-title" %>
      <%= f.input :content, as: :text, class: "article-new-text" %>
      <div data-controller="preview">
        <%= f.label "Bild hinzufügen" %>
        <%= f.input :photo, as: :file, input_html: { id:'inpFile'}, data: { action:"change->preview#connect"} %>
        <div class="image-preview" id="image-preview">
          <img src="" alt="image preview" class="image-preview__image">
          <span class="image-preview__default-text">Vorschau</span>
        </div>
      </div>
      <%= f.submit value: "Fertig" , class:"btn-info btn_form"%>
    <% end %>
  </div>
</div>
